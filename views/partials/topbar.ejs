<div class="top-bar">

<div class="tabs">

    <div class="tab">Home</div>
    <% if(isAuthenticated) { %>
        
    <div class="tab"><a href="/myblogs">My blogs</a></div>
    <% } %>
</div>
<div class="logout">
     <% if(isAuthenticated) { %>
<a id="logoutButton" href="/logout">Logout</a>
<% } else { %>
    <a id="logoutButton" href="/login">Login</a>
    <% } %>
</div>


</div>